<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="java.util.List" />

        <import type="id.langgan.android.model.Box" />

        <variable
            name="boxes"
            type="List&lt;Box>" />

        <variable
            name="plan"
            type="id.langgan.android.model.Plan" />

        <variable
            name="position"
            type="Integer" />
    </data>

    <LinearLayout
        android:id="@+id/cv_plan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="25dp"
        android:background="@color/white"
        android:orientation="horizontal"
        android:paddingLeft="8dp"
        android:paddingRight="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:orientation="vertical">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:imageUrl="@{boxes[0].products[0].photo}"
                app:visibleGone="@{boxes.size == 1 &amp;&amp; boxes[0].products.size == 1}" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                app:visibleGone="@{boxes.size == 1 &amp;&amp; boxes[0].products.size == 2}">

                <ImageView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:minHeight="100dp"
                    android:scaleType="centerCrop"
                    app:imageUrl="@{boxes[0].products[0].photo}" />

                <ImageView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:minHeight="100dp"
                    android:scaleType="centerCrop"
                    app:imageUrl="@{boxes[0].products[1].photo}" />

            </LinearLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="@{plan.delivery}"
                android:textAppearance="@style/TextAppearance.Bold14"
                android:textColor="#484848"
                tools:text="Plan Name" />

        </LinearLayout>

    </LinearLayout>

</layout>